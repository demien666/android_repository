<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demien"
    android:versionCode="1"
    android:versionName="1.0-SNAPSHOT" >
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk
        android:minSdkVersion="13"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
            >
        <!--
        android:theme="@style/AppTheme"
        -->

        <activity android:name=".notes.HelloAndroidActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".notes.SettingsActivity" android:label="@string/activitySettings">
        </activity>
        <activity android:name=".notes.AddNoteActivity" android:label="@string/activityAddNote"/>
        <activity android:name=".notes.EditNoteActivity" android:label="@string/activityEditNote"/>
        <activity android:name=".notes.InfoActivity" android:label="@string/activityInfo"/>
    </application>

</manifest>